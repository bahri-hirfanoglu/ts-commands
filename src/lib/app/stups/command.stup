import { ICommand } from "../ICommand";
import { ConsoleLog } from "../lib/logger/ConsoleLog";

export class {{class}} implements ICommand {
  /**
   * name of the command
   * @param {string}
   */
  name: string = "{{command_name}}";

  /**
   * description of the task of the command
   * @param {string}
   */
  description: string = "{{command_description}}";

  log: ConsoleLog;

  constructor() {
    this.log = new ConsoleLog();
  }
  
  /**
   * @return {void}
   */
  process(): void {}
}
